import { describe, it } from "vitest";

// Executable specification to implement during Milestone 5.
describe("TDD workflow enforcement", () => {
  it.todo("requires a failing linked test before a behavior-changing task starts");
  it.todo("allows refactoring when linked tests remain green");
  it.todo("blocks release when an acceptance criterion lacks evidence");
});
